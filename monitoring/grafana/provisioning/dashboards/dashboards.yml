apiVersion: 1

providers:
  - name: 'PizzaFlow Dashboards'
    orgId: 1
    folder: 'PizzaFlow'
    folderUid: 'pizzaflow'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
