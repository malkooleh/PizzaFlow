{{- if .Values.kitchenService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pizzaflow.fullname" . }}-kitchen-service
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "pizzaflow.labels" . | nindent 4 }}
    app: kitchen-service
    app.kubernetes.io/component: business
spec:
  replicas: {{ .Values.kitchenService.replicaCount }}
  selector:
    matchLabels:
      app: kitchen-service
  template:
    metadata:
      labels:
        app: kitchen-service
        app.kubernetes.io/component: business
        {{- include "pizzaflow.selectorLabels" . | nindent 8 }}
      annotations:
        {{- include "pizzaflow.prometheusAnnotations" (dict "port" .Values.kitchenService.service.port) | nindent 8 }}
    spec:
      {{- include "pizzaflow.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: kitchen-service
          image: "{{ .Values.global.imageRegistry }}{{ .Values.kitchenService.image.repository }}:{{ default .Values.kitchenService.image.tag .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.kitchenService.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.kitchenService.service.port }}
          envFrom:
            - configMapRef:
                name: {{ include "pizzaflow.fullname" . }}-common-config
          env:
            - name: SPRING_DATA_REDIS_HOST
              value: "{{ .Release.Name }}-redis-master"
            - name: SPRING_DATA_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-redis
                  key: redis-password
          resources:
            {{- toYaml .Values.kitchenService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.kitchenService.service.port }}
            initialDelaySeconds: 90
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.kitchenService.service.port }}
            initialDelaySeconds: 60
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pizzaflow.fullname" . }}-kitchen-service
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "pizzaflow.labels" . | nindent 4 }}
    app: kitchen-service
spec:
  type: {{ .Values.kitchenService.service.type }}
  ports:
    - port: {{ .Values.kitchenService.service.port }}
      targetPort: {{ .Values.kitchenService.service.port }}
      protocol: TCP
      name: http
  selector:
    app: kitchen-service
{{- end }}
